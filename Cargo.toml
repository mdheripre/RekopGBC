[package]
name = "rekop_gbc"
version = "0.1.0"
authors = ["mdheripre"]
edition = "2021"

[depencendies]
serde = { version = "1.0", features = ["derive"] }
clap = { version = "4", optional = true }
winit = { version = "0.29", optional = true }
